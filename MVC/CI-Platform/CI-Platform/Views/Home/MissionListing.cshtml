@using CI_Platform.Models.ViewModels
@*@model CI_Platform.Models.ViewModels.MissionListingViewModel;*@
@model CI_Platform.Models.ViewModels.MissionListingViewModel

@{
    Layout = "Home_Layout";
}

<div class="filter-bar">
    <nav class="navbar navbar-expand-md navbar-toggleable-md bg-white navbar-light bg-white border-bottom box-shadow p-0">
        <div class="container-fluid my-0">
            <div class="navbar-brand search-form">

                <input class="form-control search-input mr-2" type="search" id="search" placeholder="Search Mission..." aria-label="Search" />

            </div>

            <div class="navbar-collapsee collapse w-50 d-md-inline-flex ">
                @*<ul class="navbar-nav filters flex-grow-1 justify-content-around*@
                <div class="row gx-0 w-100 filters">
                    <div class="nav-item col-md-3 filter-item dropdown d-flex align-items-center justify-content-center">
                        <a class="nav-link dropdown-toggle text-dark header-text" data-bs-toggle="dropdown" aria-expanded="false" id="countriesMenu">
                            <span>Country</span>
                            <img src="~/images/drop-down.png" class="mx-2 dropdown-img" alt="">
                        </a>
                        <ul class="dropdown-menu dropdown-menu-sm-end" id="countries">
                            @foreach (var item in Model.Countries)
                            {
                                <li><a class="dropdown-item" href="#"><input type="checkbox" class="me-2" id="@item.Name.Replace(" ",String.Empty)" name="@item.Name" value="@item.CountryId"><label class="pe-2" for="@item.Name.Replace(" ",String.Empty)">@item.Name </label></a></li>
                            }
                            @*           <li><a class="dropdown-item" href="#">Action</a></li>
                            <li><a class="dropdown-item" href="#">Another action</a></li>
                            <li><a class="dropdown-item" href="#">Something else here</a></li>*@
                        </ul>
                    </div>
                    <li class="nav-item col-md-3 filter-item dropdown d-flex align-items-center justify-content-center">
                        <a class="nav-link dropdown-toggle text-dark header-text" data-bs-toggle="dropdown" aria-expanded="false" id="citiesMenu">
                            <span>City</span>
                            <img src="~/images/drop-down.png" class="mx-2 dropdown-img" alt="">
                        </a>
                        <ul class="dropdown-menu dropdown-menu-sm-end" id="cities">
                            @foreach (var item in Model.Cities)
                            {
                            <li>
                                    <a class="dropdown-item" href="#"><input type="checkbox" class="me-2" id="@item.Name.Replace(" ",String.Empty)" name="@item.Name" value="@item.CityId"><label class="pe-2" for="@item.Name.Replace(" ",String.Empty)">@item.Name </label></a>
                                </li>
                            }
                            @*<li><a class="dropdown-item" href="#">Action</a></li>
                            <li><a class="dropdown-item" href="#">Another action</a></li>
                            <li><a class="dropdown-item" href="#">Something else here</a></li>*@
                        </ul>
                    </li>
                    <li class="nav-item col-md-3 filter-item dropdown d-flex align-items-center justify-content-center">
                        <a class="nav-link dropdown-toggle text-dark header-text" data-bs-toggle="dropdown" aria-expanded="false">
                            <span>Theme</span>
                            <img src="~/images/drop-down.png" class="mx-2 dropdown-img" alt="">
                        </a>
                        <ul class="dropdown-menu dropdown-menu-sm-end" id="themes">
                            @foreach (var item in Model.MissionThemes)
                            {
                                <li><a class="dropdown-item" href="#"><input type="checkbox" class="me-2" id="@item.Title.Replace(" ",String.Empty)" name="@item.Title" value="@item.MissionThemeId"><label id="lb" for=@item.Title>@item.Title </label></a></li>
                            }
                            @*<li><a class="dropdown-item" href="#">Action</a></li>
                            <li><a class="dropdown-item" href="#">Another action</a></li>
                            <li><a class="dropdown-item" href="#">Something else here</a></li>*@
                        </ul>
                    </li>
                    <li class="nav-item col-md-3 filter-item dropdown d-flex align-items-center justify-content-center">
                        <a class="nav-link dropdown-toggle text-dark header-text" data-bs-toggle="dropdown" aria-expanded="false">
                            <span>Skills</span>
                            <img src="~/images/drop-down.png" class="mx-2 dropdown-img" alt="">
                        </a>
                        <ul class="dropdown-menu dropdown-menu-sm-end" id="skills">
                            @foreach (var item in Model.Skills)
                            {
                                <li><a class="dropdown-item" href="#"><input type="checkbox" class="me-2" id="@item.SkillName.Replace(" ",String.Empty)" name="@item.SkillName" value="@item.SkillId"><label for=@item.SkillName.Trim()>@item.SkillName </label></a></li>
                            }
                            @*<li><a class="dropdown-item" href="#">Action</a></li>
                            <li><a class="dropdown-item" href="#">Another action</a></li>
                            <li><a class="dropdown-item" href="#">Something else here</a></li>*@
                        </ul>
                    </li>
                    <div class="clearAllFilters filter-item align-content-center">
                        <a class="field-text" style="text-decoration:none;">Clear All Filters</a>
                    </div>

                    @* <li class="nav-item filter-item clearAllFilters d-flex align-items-center justify-content-center" id="clearAllFilters">
                    <a class="nav-link text-dark header-text" data-bs-toggle="dropdown" aria-expanded="false">
                    Clear All Filters
                    </a>
                    </li>*@
                </div>
                @*</ul>*@
            </div>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapsee" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

        </div>
    </nav>
</div>

<div class="py-3 px-4" id="addedFilters">
    <div class="row" id="selectedFilters">
      
        <a href="" id="clearAll" class="field-text my-1 col-auto" style="text-decoration:none; display:none;">Clear All</a>
    </div>
</div>

<div class="container-fluid" id="viewContainer">
    <div class="row justify-content-between px-4 align-items-center pb-4">
        <div class="col-sm-6 d-flex">
            <p style="font-size:24px;">Explore </p>&nbsp;
            <h2> 72 Missions</h2>
        </div>
        <div class="col-sm-6 d-flex  align-items-center justify-content-end" style="padding-right:0px;">
            <div class="dropdown mx-3">
                <a class="dropdown-toggle sort-by-dd text-dark text-decoration-none d-flex justify-content-between align-items-center px-2" data-bs-toggle="dropdown" aria-expanded="false">
                    <span>Sort By</span>
                    <img src="~/images/drop-down.png" class="mx-2 dropdown-img" alt="">
                </a>
                <ul class="dropdown-menu dropdown-menu-sm-end">
                    <li><a class="dropdown-item" href="#">Action</a></li>
                    <li><a class="dropdown-item" href="#">Another action</a></li>
                    <li><a class="dropdown-item" href="#">Something</a></li>
                </ul>
            </div>
            <div>
                <button id="gridButton" class="btn-grid"><img src="~/images/grid.png" /></button>
                <button id="listButton" class="btn-list"><img src="~/images/list.png" /></button>
            </div>
        </div>

    </div>
</div>


@*cards*@

@*@foreach(var item in Model.MissionCards)
{
    <li>@item.CountryId</li>
}*@
<div class="container-fluid grid-container" id="missionContainer">
    <div class="row" id="cardContainer">
@*        
        @Html.Partial("Cards");
        *@

    </div>

</div>

<div id="paginationContainer">
    <div class="pagination d-flex justify-content-center mt-5 mb-5">
        <a href="#"><img src="~/images/previous.png" alt="Previous" /></a>
        <a href="#"><img src="~/images/left.png" alt="Previous" /></a>

        <a href="#" class="active pt-1">1</a>
        <a href="#" class="pt-1">2</a>
        <a href="#" class="pt-1">3</a>
        <a href="#" class="pt-1">4</a>
        <a href="#" class="pt-1">5</a>

        <a href="#"><img src="~/images/right-arrow1.png" alt="Previous" /></a>
        <a href="#"><img src="~/images/next.png" alt="Previous" /></a>
    </div>
</div>


<div class="my-3 d-flex justify-content-center">
    <button type="button" class="Apply-mission-btn" onclick="noMissionFound()">No mission Found<img src="~/images/right-arrow.png" /></button>
</div>

<div class="no-mission-found py-5 text-center" style="display:none;" id="noMissionFound">
    <h2>
        No Mission Found
    </h2>
    <div class="my-3 d-flex justify-content-center ">
        <button type="button" class="Apply-mission-btn">Apply <img src="~/images/right-arrow.png" /></button>
    </div>
</div>

<script>
    //$(document).on('load',function(){
    //    console.log("sdff");
    //    var x = $('$countries:first-child li a input');
    //});
    
    //$('#citiesMenu').on('click', function () {
    //    async function ll() {
    //    var x =  $('#countries').find('li a input[type=checkbox]:checked');
    //    var y = $('#cities').find('li a input[type=checkbox]');
    //    //$(document).ready(function () {
            
    //    //});
        

        
    //    if(x.length == 0 && y.length == 0){
    //        console.log("df");
    //        $.ajax({
    //            url: '/Home/getAllCities',
    //            type: 'GET',
    //            data: { },
    //            dataType: 'json',
    //            success: function (data) {
                    
    //                var cities = $('#cities');
    
    //                $.each(data,function (i, item) {
    //                    var li = document.createElement('li');

    //                    var anc = document.createElement('a');
    //                    anc.classList.add("dropdown-item");

    //                    var inp = document.createElement('input');
    //                    inp.type = "checkbox";
    //                    inp.id = item.name.replaceAll(' ', '');
    //                    inp.value = item.cityId;
    //                    inp.classList.add("me-2");
                        

    //                    var lable = document.createElement('lable');
    //                    lable.classList.add("form-check-label");
    //                    lable.setAttribute("for", item.name.replaceAll(' ', ''));
    //                    lable.innerHTML = item.name;

    //                    anc.appendChild(inp);
    //                    $(inp).after($(lable));
    //                    li.appendChild(anc);
                        
    //                    cities.append(li);
    //                    //cities.append(items);
    //                 //items += `<li><a class="dropdown-item"><input type="checkbox" class="me-2" id=` + item.name.replaceAll(' ', '') + ` value=` + item.cityId + `><label class="form-check-label" for="exampleCheck1" >` + item.name + `</label></li>`;
    //                });
    //                //console.log(cities);
                    
    //                ////cities.html(items);
    //            }
    //        });
    //    }
    //    }
    //    ll().then(() => {
    //        console.log("dffffff");
    //        //var x = $('#countries').find('li a input[type=checkbox]:checked');
    //        console.log($('#cities').find('li a input[type=checkbox]').length);
    //        var y = $('#cities').find('li a input[type=checkbox]');
    //        if (y.length != 0) {
    //            console.log($('#cities').find('li a input[type=checkbox]').length);
                    
    //            fl(x);
    //        }

    //    });
    //});
    //function fl(){

        //$('#cities li a input[type=checkbox]').click(function (e) {
        //    console.log(e);
        //});
    //}

    
    //$('input[type=checkbox]').on("click", function (e) {
        
    //    if (e.currentTarget.checked) {
    //        var x = e.target.parentElement.lastChild.innerHTML;
            

    //        var main = document.createElement('div');
    //        main.classList.add("field", "col-auto", "py-1", "mb-3", "px-3");
    //        main.id = e.target.value + "1";

    //        var sname = document.createElement('span');
    //        sname.classList.add("field-text");
    //        sname.innerHTML = x;


    //        var img = $('<img />', {
    //            class: 'ps-2',
    //            src: '/images/cancel.png',
    //            alt: 'MyAlt'
    //        });

    //        main.appendChild(sname);
    //        img.appendTo(sname);
    //        document.getElementById("clearAll").style.display = "block";
    //        $(main).insertBefore('#clearAll');
    //    } else {
    //        var tag = document.getElementById(e.target.value + "1");
    //        tag.parentNode.removeChild(tag);
    //        //var total=$(this).find('input[type=checkbox]:checked').length;
    //        //console.log(length);
    //        //if(length == 0){
    //        //    document.getElementById("clearAll").style.display = "none";
    //        //}
    //    }
        

    //});
    
    //$(document).change(function(){
    //    var selectedcountries = $('#countries li a input[type=checkbox]:checked').map(function(){
    //        return this.name;
    //    }).get().join();
    //    console.log(selectedcountries);
    //    function showfilters(element,selectedcountries){
    //        $('#selectedfilters #'+element).remove();
    //    }
        
    //});

    //$(document).load(function(){
    //    var x = $('#cities').find().length;
    //    console.log(x);
    //    if(x == 0){
    //        console.log("sdes");
    //    }
    //});

    function updateFilters() {
        var selectedCountries = $('#countries li a input[type=checkbox]:checked').map(function () {
                    return {
                        name : this.name,
                        id : this.value
                    };
                });
                //console.log(selectedCountries);

        var selectedCities = $('#cities li a input[type=checkbox]:checked').map(function () {
            return {
                name: this.name,
                id: this.value
            };
        });
        //console.log(selectedCities);

        var selectedTheme = $('#themes li a input[type=checkbox]:checked').map(function () {
            return {
                name: this.name,
                id: this.value
            };
        });
        //console.log(selectedtheme);

        var selectedSkill = $('#skills li a input[type=checkbox]:checked').map(function () {
            return {
                name: this.name,
                id: this.value
            };
        });
        //console.log(selectedskill);
        var filters = $('#selectedFilters');
        filters.empty();
        addSelected(selectedCountries);
        addSelected(selectedCities);
        addSelected(selectedTheme);
        addSelected(selectedSkill);

        var clearall = document.createElement("a");
        clearall.id = "clearALl";
        clearall.classList.add("field-text","my-1","col-auto");
        clearall.style.textDecoration = "none";
        clearall.innerHTML = "Clear All";
        clearall.onclick = clearAll;
        
        
        if ($('#selectedFilters').find('div').length != 0) {
            filters.append(clearall);
        }

    }
    
    function addSelected(selected){
        selected.each(function(){
            var filters = $('#selectedFilters');
            //cityName = currentCity.substring(0, currentCity.indexOf("_"));
            //console.log(this.name);
            //console.log(this.id);

            var main = document.createElement('div');
            main.classList.add("field", "col-auto", "py-1", "mb-3", "px-3");
            main.id = this.name.replaceAll(" ", "");

            var sname = document.createElement('span');
            sname.classList.add("field-text");
            sname.innerHTML = this.name;


            var img = $('<img />', {
                class: 'ps-2',
                src: '/images/cancel.png',
                alt: 'MyAlt'
            });

            main.appendChild(sname);
            img.appendTo(sname);
            filters.append(main);
        });
    }
    $(document).change(function(){
        var selectedCountries = $('#countries li a input[type=checkbox]:checked').map(function () {
            return {
                name: this.name,
                id: this.value
            };
        });
        var selectedCountryID = $('#countries li a input[type=checkbox]:checked').map(function () {
            return this.value;
            
        }).get().join();
        var selectedCities = $('#cities li a input[type=checkbox]:checked').map(function () {
            return {
                name: this.name,
                id: this.value
            };
        });
        console.log(selectedCountryID);
        $.ajax({
            url: '/Home/GetCards',
            type: 'POST',
            data: {CountryIDs: selectedCountryID },
            dataType: 'html',
            success:function (viewModel) {
                //console.log(viewModel);
                
                $('#cardContainer').html(viewModel);
            }
        });
        //$('#missionContainer div').find('#passMissionCard #card').each(function () {
            
        //    var currCity = $(this).find('#passCity').text();
        //    var currCountry = $(this).find('#passCountry').text();
        //    var currTheme = $(this).find('#PassMissionTheme').text();


        //    if(selectedCountries.length == 0 && selectedCities.length == 0){
        //         $(this).closest('#passMissionCard').show();
        //    }
        //    else if(selectedCountries.length == 0 && selectedCities.length != 0){
        //        if (minOneSelected(selectedCities) == true){
        //            if (matchCountry(currCity, selectedCities) != true) {
        //                //console.log("not matched");
        //                $(this).closest('#passMissionCard').hide();
        //            } else {
        //                //console.log($(this).closest('#passMissionCard'));
        //                $(this).closest('#passMissionCard').show();
        //            }
        //        }
        //    }else if(selectedCountries.length != 0 && selectedCities.length == 0){
        //        if (minOneSelected(selectedCountries) == true){
        //            if (matchCountry(currCountry, selectedCountries) != true) {
        //                //$(this).hide();
        //                $(this).closest('#passMissionCard').hide();
        //                //console.log($(this).closest('#passMissionCard'));
        //            } else {

        //                $(this).closest('#passMissionCard').show();
        //            }
        //        }
        //    }
        //    else{
        //        if (minOneSelected(selectedCountries) == true){
        //            if (matchCountry(currCountry, selectedCountries) != true) {
        //                //$(this).hide();
        //                $(this).closest('#passMissionCard').hide();
        //                //console.log($(this).closest('#passMissionCard'));
        //            } else {

        //                $(this).closest('#passMissionCard').show();
        //            }
        //        }
            
        //        //console.log(currCity);
        //        //console.log(selectedCities.length);
        //        if (minOneSelected(selectedCities) == true){
        //            if (matchCountry(currCity, selectedCities) != true) {
        //                //console.log("not matched");
        //                $(this).closest('#passMissionCard').hide();
        //            } else {
        //                //console.log($(this).closest('#passMissionCard'))
        //                $(this).closest('#passMissionCard').show();
        //            }
        //        }
        //    }
                        
        //});
        
        updateFilters();
     
    });
    function minOneSelected(selected){
        if(selected.length !=0){
            return true;
        }else{
            return false;
        }
    }
    function matchCountry(currCountry, selectedCountries){
        
        for(var item of selectedCountries){
            if(item.name == currCountry){
                return true;
            }
        }
         return false;  
        
    }
    $(document).ready(function () {
        
        $.ajax({
            url: '/Home/GetCards',
            type: 'POST',
            data: {CountryIDs: ""},
            dataType: 'html',
            success:function (viewModel) {
                console.log("All");
                $('#cardContainer').html(viewModel);
            }
        });
       

        $('#countries li a input[type=checkbox]').click(function (e) {
            var CountryId = $(this).val();
            console.log(CountryId);
            //console.log(CountryId.substring(CountryId.indexOf("-")+1));
            ////console.log(CountryId);
            //var CountryId = parseInt(CountryId);
            if (e.currentTarget.checked) {

                $.ajax({
                    url: '/Home/GetCityByCountry',
                    type: 'GET',
                    data: { CountryId: CountryId },
                    dataType: 'json',
                    success:function (data) {
                        var cities = $('#cities');
                        if($('#countries').find('li a input[type=checkbox]:checked').length == 1){
                            cities.empty();
                        }
                        //cities.empty();
                        //var items = "";
                        $.each(data, function (i, item) {
                            var li = document.createElement('li');

                            var anc = document.createElement('a');
                            anc.classList.add("dropdown-item");

                            var inp = document.createElement('input');
                            inp.type = "checkbox";
                            inp.id = item.name.replaceAll(' ', '');
                            inp.value = item.cityId;
                            inp.name = item.name
                            inp.classList.add("me-2");


                            var lable = document.createElement('lable');
                            lable.classList.add("form-check-label");
                            lable.setAttribute("for", item.name.replaceAll(' ', ''));
                            lable.innerHTML = item.name;

                            anc.appendChild(inp);
                            $(inp).after($(lable));
                            li.appendChild(anc);

                            cities.append(li);
                            //items += `<li><a class="dropdown-item"><input type="checkbox" class="me-2" id=` + item.name.replaceAll(' ', '') + ` value=` + item.cityId + `><label class="form-check-label" for="exampleCheck1" >` + item.name + `</label></li>`
                            
                        });                        
                        //selectCities();                        
                    }
                });
                
            } else {
                $.ajax({
                    url: '/Home/GetCityByCountry',
                    type: 'GET',
                    data: { CountryId: CountryId },
                    dataType: 'json',
                    success: function (data) {
                        var cities = $('#cities');

                        var items = "";
                        $.each(data, function (i, item) {
                            var id = "#" + item.name.replaceAll(' ', '');
                            console.log(id);
                            $(id).parent().parent().remove();
                        });

                    }
                });
            }
            
        });
    });
    function selectCities(){
        $('#cities li a input[type=checkbox]').one('click',function (e) {
            

            if(e.currentTarget.checked){
                
                console.log(e.target.id);
                console.log($('#cities li a').find(e.id).length);
                //var x = await P
                if($('#cities li a').find(e.target.id).length == 0){
                    var x = e.target.parentElement.lastChild.innerHTML;


                    var main = document.createElement('div');
                    main.classList.add("field", "col-auto", "py-1", "mb-3", "px-3");
                    main.id = e.target.value + "1";

                    var sname = document.createElement('span');
                    sname.classList.add("field-text");
                    sname.innerHTML = x;


                    var img = $('<img />', {
                        class: 'ps-2',
                        src: '/images/cancel.png',
                        alt: 'MyAlt'
                    });

                    main.appendChild(sname);
                    img.appendTo(sname);
                    document.getElementById("clearAll").style.display = "block";
                    $(main).insertBefore('#clearAll');
                }
                
            }
        });

        
    }

    

</script>